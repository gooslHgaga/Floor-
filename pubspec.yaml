name: adan
description: تطبيق أوقات الأذان
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5
  adhan_dart: ^1.1.2
  intl: ^0.18.1
  hijri: ^3.0.0
  flutter_local_notifications: ^18.0.1
  file_picker: ^10.3.1
  audioplayers: ^6.5.0
  flutter_analog_clock: ^1.0.3
  google_fonts: ^6.2.1
  geolocator: ^13.0.1
  shared_preferences: ^2.3.2
  flutter_native_timezone: ^2.0.0
  timezone: ^0.9.4   # متوافق مع notifications

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
